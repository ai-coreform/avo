/**
 * Public types exported for use in other apps (admin, mobile)
 * These are specifically for Hono RPC client type inference
 */

// Re-export the app type for RPC clients
export type { AppType } from "./app";
